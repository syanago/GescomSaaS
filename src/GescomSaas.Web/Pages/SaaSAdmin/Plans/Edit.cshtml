@page "{id:guid}"
@model GescomSaas.Web.Pages.SaaSAdmin.Plans.EditModel
@{
    ViewData["Title"] = "Edition plan";
}

<div class="page-header">
    <div>
        <h1 class="page-header-title">Editer le plan @Model.Input.Code</h1>
        <div class="page-header-sub">Mettez a jour les limites et les tarifs du plan.</div>
    </div>
    <div>
        <a class="btn btn-secondary" asp-page="/SaaSAdmin/Plans/Index"><i class="bi bi-arrow-left me-1"></i>Retour</a>
    </div>
</div>

<div class="card">
    <div class="section-header"><i class="bi bi-tag"></i><span>Informations du plan</span></div>
    <div class="card-body">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
            <partial name="_PlanForm" model="Model.Input" />
            <div class="d-flex justify-content-end gap-2 mt-3">
                <a class="btn btn-secondary" asp-page="/SaaSAdmin/Plans/Index">Annuler</a>
                <button type="submit" class="btn btn-primary"><i class="bi bi-save me-1"></i>Enregistrer</button>
            </div>
        </form>
    </div>
</div>
