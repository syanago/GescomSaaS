@{
    // Layout dédié pour les pages d'authentification (login, register, forgot password).
    // Utilise les tokens Untitled UI strict, pas de sidebar/header d'app.
    Layout = "/Pages/Shared/_LayoutAuth.cshtml";
}
